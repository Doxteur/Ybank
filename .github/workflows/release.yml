name: Release
on:
  push:
    branches:
      - main

# Ajouter les permissions
permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  sonar:
    name: SonarQube Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=Ybank
            -Dsonar.projectName=Ybank

  release:
    needs: sonar
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./client
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: './client/yarn.lock'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_ENV: production
        run: yarn semantic-release

  docker:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push frontend
        uses: docker/build-push-action@v4
        with:
          context: ./client
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/ynovbank-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/ynovbank-frontend:${{ github.sha }}

      - name: Build and push backend
        uses: docker/build-push-action@v4
        with:
          context: ./server
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/ynovbank-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/ynovbank-backend:${{ github.sha }}

  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SERVER_SSH_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}

      - name: Deploy to server
        run: |
          ssh root@92.112.192.183 '
            cd /app/Ybank &&
            git remote set-url origin git@github.com:Doxteur/Ybank.git &&
            git pull &&
            docker compose pull &&
            docker compose up -d
          '

  notify:
    needs: deploy
    runs-on: ubuntu-latest
    if: always()  # S'exécute même si le déploiement échoue
    steps:
      - name: Send notification email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.GMAIL_USERNAME }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "🚀 Ybank - Déploiement ${{ job.status }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: "Ybank CI/CD <${{ secrets.GMAIL_USERNAME }}>"
          body: |
            <div style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto;">
              <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 20px; border-radius: 8px 8px 0 0;">
                <h1 style="margin: 0;">🚀 Ybank CI/CD</h1>
                <p style="margin: 10px 0 0 0; opacity: 0.9;">Rapport de déploiement automatique</p>
              </div>

              <div style="padding: 20px; border: 1px solid #e9ecef; border-radius: 0 0 8px 8px;">
                <h3>📋 Informations du déploiement</h3>
                <table style="width: 100%; border-collapse: collapse; margin-bottom: 20px;">
                  <tr><td style="padding: 8px; border-bottom: 1px solid #eee;"><strong>Commit :</strong></td><td style="padding: 8px; border-bottom: 1px solid #eee;">${{ github.sha }}</td></tr>
                  <tr><td style="padding: 8px; border-bottom: 1px solid #eee;"><strong>Branche :</strong></td><td style="padding: 8px; border-bottom: 1px solid #eee;">${{ github.ref_name }}</td></tr>
                  <tr><td style="padding: 8px; border-bottom: 1px solid #eee;"><strong>Déclencheur :</strong></td><td style="padding: 8px; border-bottom: 1px solid #eee;">${{ github.actor }}</td></tr>
                  <tr><td style="padding: 8px; border-bottom: 1px solid #eee;"><strong>Date :</strong></td><td style="padding: 8px; border-bottom: 1px solid #eee;">${{ github.event.head_commit.timestamp }}</td></tr>
                </table>

                <h3>🔧 Étapes de la pipeline</h3>
                <ul style="list-style: none; padding: 0;">
                  <li style="padding: 8px 0; border-bottom: 1px solid #f8f9fa;">✅ Analyse SonarQube</li>
                  <li style="padding: 8px 0; border-bottom: 1px solid #f8f9fa;">✅ Release automatique</li>
                  <li style="padding: 8px 0; border-bottom: 1px solid #f8f9fa;">✅ Build et push Docker</li>
                  <li style="padding: 8px 0; border-bottom: 1px solid #f8f9fa;">✅ Déploiement serveur</li>
                </ul>

                <h3>🐳 Images Docker déployées</h3>
                <div style="background: #f8f9fa; padding: 15px; border-radius: 4px; margin-bottom: 20px;">
                  <p style="margin: 5px 0;"><strong>Frontend:</strong> ${{ secrets.DOCKER_USERNAME }}/ynovbank-frontend:${{ github.sha }}</p>
                  <p style="margin: 5px 0;"><strong>Backend:</strong> ${{ secrets.DOCKER_USERNAME }}/ynovbank-backend:${{ github.sha }}</p>
                </div>

                <h3>🌐 Serveur de production</h3>
                <p><strong>IP :</strong> 92.112.192.183</p>

                <hr style="border: none; border-top: 1px solid #e9ecef; margin: 20px 0;">
                <p style="color: #6c757d; font-size: 12px; text-align: center;">
                  Email automatique généré par GitHub Actions - Ybank CI/CD Pipeline
                </p>
              </div>
            </div>
